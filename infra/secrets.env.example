# Infrastructure secrets for automated deployment
# Copy to secrets.env and fill in your values:
#   cp secrets.env.example secrets.env
#
# WARNING: secrets.env contains sensitive data - it's gitignored

# === Hetzner ===
HETZNER_LOCATION=fsn1              # fsn1 (Falkenstein), nbg1 (Nuremberg), hel1 (Helsinki)
HETZNER_SERVER_TYPE=cx22           # cx22 (2 vCPU, 4GB) or cx32 (4 vCPU, 8GB)

# === Domain & DNS ===
DOMAIN=yourdomain.com              # Your domain (e.g., example.com)
SUBDOMAIN=assistant                # Subdomain for the app (e.g., assistant.example.com)
CLOUDFLARE_API_TOKEN=              # API token with "Edit zone DNS" permission
CLOUDFLARE_ZONE_ID=                # Dashboard → your domain → right sidebar → API section

# === Tailscale ===
TS_OAUTH_SECRET=                   # OAuth client secret (tskey-client-...)
TS_TAG=tag:server                  # Tag for the device (must exist in ACLs)

# === GitHub ===
GH_REPO=username/assistant         # GitHub repo path (e.g., myuser/assistant)

# === Telegram ===
TELEGRAM_BOT_TOKEN=                # Bot token from @BotFather

# === OpenAI ===
OPENAI_API_KEY=                    # OpenAI API key (for embeddings)

# === Auto-generated if empty ===
# Leave these empty and deploy.sh will generate secure random values
ANTHROPIC_PROXY_SESSION_SECRET=    # 32-byte hex for session encryption
TELEGRAM_WEBHOOK_SECRET_TOKEN=     # 16-byte hex for webhook verification
LETTA_SERVER_PASSWORD=             # 16-byte hex for Letta API auth

# === Optional ===
NETDATA_CLAIM_TOKEN=               # Netdata Cloud claim token (for remote monitoring)
