# Copy to Caddyfile and customize:
#   cp Caddyfile.example Caddyfile
#   nano Caddyfile

# Main app - Telegram webhook
assistant.mosphere.at {
    reverse_proxy app:3000
}

# Letta ADE - password protected by Letta itself (LETTA_SERVER_PASSWORD)
# Connect via https://app.letta.com → Self-Hosted Server
letta.assistant.mosphere.at {
    reverse_proxy letta:8283
}

# Netdata is accessed via Tailscale (http://TAILSCALE_IP:19999)
# No public exposure needed
