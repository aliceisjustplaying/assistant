# Copy to Caddyfile and customize:
#   cp Caddyfile.example Caddyfile
#   nano Caddyfile
#
# Note: deploy.sh generates this automatically with your domain

# Main app - Telegram webhook
assistant.yourdomain.com {
    reverse_proxy app:3000
}

# Internal services accessed via Tailscale only (no public exposure):
#   http://TAILSCALE_IP:8283  - Letta API/ADE
#   http://TAILSCALE_IP:19999 - Netdata monitoring
#   http://TAILSCALE_IP:4001  - Anthropic Proxy (OAuth setup)
